"    def rank(\n        self,\n        query: str,\n        candidates: List[Dict[str, Any]],\n        context: Dict[str, Any] = None,\n        session=None\n    ) -> List[Dict[str, Any]]:\n        \"\"\"\n        Ranks a list of candidate retrieval results and returns them sorted descending.\n        Candidates should be list of {\"score\": float, \"chunk\": dict} items.\n        \"\"\"\n        if not candidates:\n            return []\n\n        if session is None:\n            db_session = SessionLocal()\n            close_session = True\n        else:\n            db_session = session\n            close_session = False\n\n        ranked = []\n\n        try:\n            # Batch retrieve database metadata for candidates to optimize query performance\n            file_paths = {c[\"chunk\"].get(\"file\") for c in candidates if c[\"chunk\"].get(\"file\")}\n            symbol_names = {c[\"chunk\"].get(\"name\") for c in candidates if c[\"chunk\"].get(\"name\")}\n\n            db_files = {}\n            if file_paths:\n                rows = db_session.query(File).filter(File.path.in_(list(file_paths))).all()\n                db_files = {f.path: f for f in rows}\n\n            db_symbols = {}\n            if symbol_names:\n                rows = db_session.query(Symbol).filter(Symbol.qualified_name.in_(list(symbol_names))).all()\n                db_symbols = {s.qualified_name: s for s in rows}\n\n            for item in candidates:\n                chunk = item[\"chunk\"]\n                \n                # Retrieve matching DB records from cache\n                fpath = chunk.get(\"file\")\n                file_row = db_files.get(fpath) if fpath else None\n                \n                sname = chunk.get(\"name\")\n                symbol_row = db_symbols.get(sname) if sname else None\n\n                # 1. Embedding Score (0 - 100)\n                emb_score = self.embedding_score(item)\n\n                # 2. Graph Score (0 - 50)\n                g_score = self.graph_score(db_session, symbol_row, context)\n
<truncated 1532 bytes>