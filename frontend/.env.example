# Copy to .env.local for local dev (Vite reads VITE_* only).
VITE_KEYCLOAK_URL=http://localhost:8081
VITE_KEYCLOAK_REALM=hrms
VITE_KEYCLOAK_CLIENT_ID=hrms-frontend
# Leave empty to use same-origin /api via Vite dev proxy (see vite.config.ts).
VITE_API_BASE_URL=
