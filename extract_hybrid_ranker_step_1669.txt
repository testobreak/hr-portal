"    def recency_score(self, file_row: File | None) -> float:\n        \"\"\"\n        Gives a recency bonus of up to 20 points based on elapsed hours.\n        \"\"\"\n        updated_at = None\n        if file_row:\n            updated_at = getattr(file_row, \"updated_at\", None) or getattr(file_row, \"modified_at\", None)\n\n        if not updated_at:\n            return 0.0\n\n        if updated_at.tzinfo is None:\n            updated_at = updated_at.replace(tzinfo=timezone.utc)\n\n        now = datetime.now(timezone.utc)\n        hours = (now - updated_at).total_seconds() / 3600.0\n\n        if hours < 1.0:\n            return 20.0\n        elif hours < 24.0:\n            return 10.0\n        return 0.0"