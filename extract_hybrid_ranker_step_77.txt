"        try:\n            # 1. Parse intent target symbols\n            targets = self.extract_target_symbols(query)\n            \n            # 2. Get active task_id safely for historical scoring\n            task_id = \"unknown_task\"\n            if context and context.get(\"task_id\"):\n                task_id = context.get(\"task_id\")\n            \n            # Get goal safely from context or fallback to scanning the tasks directory\n            goal = \"\"\n            if context and context.get(\"goal\"):\n                goal = context.get(\"goal\")\n                \n            if not goal:\n                try:\n                    from pathlib import Path\n                    import json\n                    tasks_dir = Path(\".agent/tasks\")\n                    if not tasks_dir.exists() and Path(\"tasks\").exists():\n                        tasks_dir = Path(\"tasks\")\n                    if tasks_dir.exists():\n                        task_files = sorted(\n                            tasks_dir.glob(\"*.json\"),\n                            key=lambda p: p.stat().st_mtime,\n                            reverse=True\n                        )\n                        task_files = [f for f in task_files if not f.name.endswith(\".backup.json\")]\n                        if task_files:\n                            with open(task_files[0], \"r\", encoding=\"utf-8\") as f:\n                                task_data = json.load(f)\n                                goal = task_data.get(\"goal\", \"\")\n                except Exception as e:\n                    print(f\"[HybridRanker] Fallback goal detection failed: {e}\")\n\n            # Batch retrieve database metadata for candidates to optimize performance\n            file_paths = {c[\"chunk\"].get(\"file\") for c in candidates if c[\"chunk\"].get(\"file\")}\n            symbol_names = {\n                c[\"chunk\"].get(\"symbol\") or c[\"chunk\"].get(\"name\")\n                for c in candidates\n                if c[\"chunk\"].get(\"symbol\") or
<truncated 6082 bytes>