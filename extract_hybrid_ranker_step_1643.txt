"import os\nimport sys\nfrom datetime import datetime, timezone\nfrom typing import List, Dict, Any\n\nPROJECT_ROOT = os.path.abspath(\n    os.path.join(\n        os.path.dirname(__file__),\n        \"..\",\n        \"..\"\n    )\n)\nif PROJECT_ROOT not in sys.path:\n    sys.path.insert(0, PROJECT_ROOT)\n\nfrom repository_intelligence.db.connection import SessionLocal\nfrom repository_intelligence.models.file import File\nfrom repository_intelligence.models.symbol import Symbol, SymbolRelationship\n\n\nclass HybridRanker:\n    \"\"\"\n    Enterprise Hybrid Ranker that scores and ranks retrieval candidates \n    using 5 distinct retrieval signals:\n      - Embedding match score (45%)\n      - Graph connection score (25%)\n      - Symbol resolver match score (15%)\n      - File importance centrality score (10%)\n      - Recency/freshness score (5%)\n    \"\"\"\n\n    def __init__(self):\n        self.embedding_weight = 0.45\n        self.graph_weight = 0.25\n        self.symbol_weight = 0.15\n        self.file_importance_weight = 0.10\n        self.recency_weight = 0.05\n\n    def rank(\n        self,\n        query: str,\n        candidates: List[Dict[str, Any]],\n        context: Dict[str, Any] = None\n    ) -> List[Dict[str, Any]]:\n        \"\"\"\n        Ranks a list of candidate retrieval results and returns them sorted descending.\n        Candidates should be list of {\"score\": float, \"chunk\": dict} items.\n        \"\"\"\n        if not candidates:\n            return []\n\n        session = SessionLocal()\n        ranked = []\n\n        try:\n            # Batch retrieve database metadata for candidates to optimize query performance\n            file_paths = {c[\"chunk\"].get(\"file\") for c in candidates if c[\"chunk\"].get(\"file\")}\n            symbol_names = {c[\"chunk\"].get(\"name\") for c in candidates if c[\"chunk\"].get(\"name\")}\n\n            db_files = {}\n            if file_paths:\n                rows = session.query(File).filter(File.path.in_(list(file_paths))).all()\n                
<truncated 6844 bytes>