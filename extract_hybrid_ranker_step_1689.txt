"        # General codebase connectivity fallback\n        inbound = session.query(SymbolRelationship).filter(SymbolRelationship.target_symbol_id == symbol.id).count()\n        outbound = session.query(SymbolRelationship).filter(SymbolRelationship.source_symbol_id == symbol.id).count()\n        total_connections = inbound + outbound\n        return min(50.0, total_connections * 15.0)\n\n    def file_importance_score(self, session, file_row: File | None) -> float:\n        \"\"\"\n        Calculates code centrality score bounded to 0-50 based on inbound/outbound references.\n        \"\"\"\n        if not file_row:\n            return 0.0\n\n        # symbols defined in this file\n        symbol_count = session.query(Symbol.id).filter(Symbol.file_id == file_row.id).count()\n\n        # Inbound references targeting symbols in this file\n        inbound_refs = session.query(SymbolRelationship.id).filter(\n            SymbolRelationship.target_symbol_id.in_(\n                session.query(Symbol.id).filter(Symbol.file_id == file_row.id)\n            )\n        ).count()\n\n        # Outbound references originating from symbols in this file\n        outbound_refs = session.query(SymbolRelationship.id).filter(\n            SymbolRelationship.source_file_id == file_row.id\n        ).count()\n\n        importance = inbound_refs + outbound_refs + symbol_count\n        # Normalize centrality score to range [0.0, 50.0]\n        return min(50.0, importance * 5.0)\n\n    def recency_score(self, file_row: File | None) -> float:\n        \"\"\"\n        Gives a recency bonus of up to 20 points based on elapsed hours.\n        \"\"\"\n        updated_at = None\n        if file_row:\n            updated_at = getattr(file_row, \"updated_at\", None) or getattr(file_row, \"modified_at\", None)\n\n        if not updated_at:\n            return 0.0\n\n        if updated_at.tzinfo is None:\n            updated_at = updated_at.replace(tzinfo=timezone.utc)\n\n        now = datetime.now(timezone.utc)\n        hours = (now - updated_at).to
<truncated 262 bytes>